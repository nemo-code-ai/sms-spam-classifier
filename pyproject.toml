[project]
name = "sms-spam-classifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nemo-code-ai", email = "themichaelugwumbanwoye@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.1",
    "packaging>=26.3",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
    "streamlit>=1.63.0",
]

[project.scripts]
sms-spam-classifier = "sms_spam_classifier:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
